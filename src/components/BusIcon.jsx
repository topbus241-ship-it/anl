import React from 'react'
import { Bus } from 'lucide-react'

export default function BusIcon({ size = 32, color = '#0f172a', strokeWidth = 1.6 }) {
  return <Bus size={size} color={color} strokeWidth={strokeWidth} aria-hidden="true" />
}
